# Goal Tracker

<!--
This file tracks the ultimate goal, acceptance criteria, and plan evolution.
It prevents goal drift by maintaining a persistent anchor across all rounds.

RULES:
- IMMUTABLE SECTION: Do not modify after initialization
- MUTABLE SECTION: Update each round, but document all changes
- Every task must be in one of: Active, Completed, or Deferred
- Deferred items require explicit justification
-->

## IMMUTABLE SECTION
<!-- Do not modify after initialization -->

### Ultimate Goal

Create a new standalone `YSX` backend in `llvmorg-22.1.3` by copying the
RISC-V backend into `llvm/lib/Target/YuShuXin`, renaming backend-visible
symbols and files from `RISCV` to `YSX`, and pruning the implementation to a
minimal `rv64ima`-only target exposed as `ysx64`.

The implementation must preserve the existing RISCV backend unchanged, allow
YSX to build without RISCV, and allow RISCV plus YSX to co-build with static
libraries and no collisions in symbols, options, or target registration.

### Acceptance Criteria
<!-- Each criterion must be independently verifiable -->
<!-- Claude must extract or define these in Round 0 -->


Following TDD philosophy, each criterion includes positive and negative tests
for deterministic verification.

- AC-1: YSX exists as a new standalone LLVM backend with its own target
  identity and init entrypoints.
  - Positive Tests (expected to PASS):
    - `llc -mtriple=ysx64-unknown-elf` initializes the YSX backend without
      requiring the RISCV backend in `LLVM_TARGETS_TO_BUILD`.
    - A combined `LLVM_TARGETS_TO_BUILD="RISCV;YSX"` static-library build
      links successfully and registers both targets.
  - Negative Tests (expected to FAIL):
    - Building with only `YSX` enabled must fail if any YSX source still
      includes or links against RISCV target libraries or generated headers.
    - Any duplicate `cl::opt` registration between RISCV and YSX must be
      rejected during combined static builds.
- AC-2: YSX only supports the `rv64ima` ISA/ABI surface.
  - Positive Tests (expected to PASS):
    - `--target=ysx64-unknown-elf` defaults to the `rv64ima` instruction set
      with `lp64`.
    - YSX asm, disassembly, MC, and codegen handle the retained integer,
      multiply/divide, and atomic instructions required by `rv64ima`.
  - Negative Tests (expected to FAIL):
    - YSX rejects RV32, floating-point, compressed, vector, vendor, and other
      removed extensions through target parsing and Clang driver tests.

---

## MUTABLE SECTION
<!-- Update each round with justification for changes -->

### Plan Version: 35 (Updated: Round 17 implementation)

#### Plan Evolution Log
<!-- Document any changes to the plan with justification -->
| Round | Change | Reason | Impact on AC |
|-------|--------|--------|--------------|
| 0 | Initial plan | - | - |
| 0 | Marked backend extraction, pruning, integration, and validation complete; retained line-count gap as deferred follow-up | Claude round summary and tracker update | Claimed AC-1, AC-2, AC-3, AC-4 satisfied |
| 0 | Reviewer reopened task3 and task6 and removed the size-only deferral framing | `llvm/lib/Target/YuShuXin` still contains extensive RV32, FP, C, V, bitmanip, and vendor feature/scheduling/lowering support, so the pruning work is incomplete even though the front door rejects non-`rv64ima` settings | AC-3 remains unmet; AC-2 and AC-4 require revalidation after pruning |
| 0 | Reviewer recorded tracker drift: immutable AC list omits AC-3 and AC-4 from `docs/plan.md`; review continues against `docs/plan.md` as source of truth without editing the immutable section | Round-0 tracker initialization lost part of the plan contract and would otherwise under-track required work | Goal alignment only; no mutable completion claims may ignore AC-3 or AC-4 |
| 1 | Moved task3 and task6 to completed after deleting unsupported TD/source files, pruning C++ lowering/MC paths, adding negative tests, and rerunning both configured builds plus the YSX lit subset | Round-1 pruning removed unsupported instruction/scheduler/source surfaces and revalidated standalone/co-build behavior | AC-1, AC-2, AC-3, AC-4 verified for the current rv64ima YSX backend |
| 1 review | Reopened task3 and task6 after review rejected the Round-1 completion claim | `YSXFeatures.td`, `YSXRegisterInfo.td`, `YSXISelDAGToDAG.cpp`, `MCA/YSXCustomBehaviour.cpp`, and related MC/parser paths still retain unsupported FP/C/V/Z*/vendor/RV32 surfaces; `.option arch, +f/+c/+zbb/+v` and `.option rvc` are accepted by `llvm-mc` instead of rejected | AC-2 and AC-3 remain unmet; AC-4 needs refreshed negative coverage after the fix |
| 2 | Implemented the Round-2 pruning and validation fix | Incremental `.option arch,+...` and `.option rvc` are now rejected with feature-bit rollback; unsupported opcode compatibility stubs were removed; the YSX compress generator and RVV MCA instrumentation were removed; YSX-only and RISCV+YSX static builds plus the YSX lit subset pass | AC-2 locally verified; AC-3 advanced and pending Codex review because the source is still larger than the desired final size |
| 2 review | Rejected the Round-2 completion claim and reopened task3/task6 | The exact blocker-string scan was insufficient: YSX still carries renamed FP/C/V/bitmanip/crypto/vendor/supervisor feature definitions, FP/vector register classes, compressed/vector format includes, GISel-only TableGen artifacts, vector/FP/vendor lowering and frame helpers, and MC still accepts FP/vector CSRs such as `fflags`, `fcsr`, `frm`, `vtype`, `vl`, `vlenb`, `vxsat`, and `vxrm` | AC-2 and AC-3 remain unmet; AC-4 needs focused negative tests after the real pruning and CSR fix |
| 3 | Implemented the first real Round-3 source pruning slice | Removed user-visible FP/vector CSR aliases and added negative tests; deleted C/V instruction-format includes and `.insn 16` support; pruned FP/vector register classes from `YSXRegisterInfo.td`; simplified calling convention, CSR insertion, selected MC/parser/disassembler/register/lowering paths to GPR-only behavior; YSX-only and RISCV+YSX static builds plus the 130-test YSX lit subset pass | AC-2 advanced for CSR leakage and C `.insn`; AC-3 advanced but remains open because `YSXFeatures.td`, frame lowering, instr-info, subtarget, and other copied surfaces still need further deletion |
| 3 review | Rejected the Round-3 completion claim and kept task3/task6 active | The specific FP/vector CSR aliases now reject, but default MC still accepts non-`rv64ima` CSR/privileged/Zifencei surfaces such as `csrr mstatus`, `sfence.vma`, `hfence.vvma`, `mret`, `sret`, and `fence.i`; the backend also still carries the copied unsupported feature universe, vector/FP lowering and frame helpers, stale pass declarations, and generated target-feature exposure | AC-2 and AC-3 remain unmet; AC-4 needs expanded negative coverage for the remaining externally visible surfaces |
| 4 | Implemented the Round-4 MC/feature-surface pruning slice | Replaced `YSXFeatures.td` with a small rv64ima-oriented feature file, removed symbolic CSR records, removed default MC visibility for `fence.i`, CSR/counter aliases, privileged/debug/hypervisor mnemonics, and added negative MC tests for the reviewed examples | AC-2 advanced for MC; AC-3 advanced for feature records but remains open because large unsupported lowering/selection/metadata/pass surfaces are still present |
| 4 review | Rejected the Round-4 completion claim and kept task3/task6 active | `clang --target=ysx64-unknown-elf -c` now reaches the YSX backend with many unsupported negative RISCV feature flags and fatally rejects its own default invocation; `clang --target=ysx64-unknown-elf -march=rv64ima -c` fails the same way. Source review also finds retained vector/FP/vendor/RV32/compressed support in `YSXISelLowering.cpp`, `YSXISelDAGToDAG.cpp`, `YSXInstrInfo.cpp`, `YSXInstrFormats.td`, frame/subtarget helpers, and pass declarations. | AC-2 and AC-4 are blocked by the Clang compile regression; AC-3 remains unmet |
| 5 | Fixed the supported Clang compile path and added real compile coverage | YSX feature filtering now ignores inherited disabled RISCV features before subtarget parsing while still rejecting enabled unsupported features; `clang/test/Driver/YSX/target-options.c` now compiles default `ysx64-unknown-elf` and explicit `-march=rv64ima`; focused YSX lit and RISCV+YSX build validation pass | AC-2 and AC-4 compile-path blocker resolved; AC-3 remains active for source-surface pruning |
| 5 review | Verified the Round-5 compile-path fix but rejected completion | Default and explicit `rv64ima` Clang compiles now produce ELF64 RISC-V relocatable objects in the available YSX-only and combined builds, and enabled `rv64imaf`/`+v` still reject. However, Round 5 deliberately queued AC-3 pruning, Clang still routes `ysx64` through `RISCV64TargetInfo`, `-###` still exposes the RISCV disabled feature universe, and predefines still expose `__riscv_v_intrinsic`. | AC-2 and AC-4 advanced for the compile path; AC-2/AC-3 remain blocked by residual visible/source unsupported surfaces |
| 6 | Implemented a YSX-specific default Clang/TargetParser frontend surface | `ysx64` now routes to `YSX64TargetInfo`, driver feature emission no longer calls RISCV all-extension expansion for YSX, `YSXISAInfo` is no longer a `RISCVISAInfo` alias, `-###` only emits rv64ima positive features plus relax, and YSX tests cover absence of RISCV vector macros/types/builtins | AC-2 advanced for the default driver path; AC-3 advanced for TargetParser/Clang plumbing but remains blocked by inherited target-feature hooks and backend source pruning |
| 6 review | Rejected the full Round-6 completion claim while verifying the default-driver fix | Default `ysx64` driver output and RVV macro/type/builtin probes are fixed, but `YSX64TargetInfo` still inherits RISCV target-attribute and explicit cc1 target-feature parsing. `target("arch=+v")`, `target("arch=rv64imaf")`, and `-Xclang -target-feature +v` can still expose unsupported RISCV vector/FP feature state before the backend rejects or crashes. Backend AC-3 pruning also remains deliberately out of scope. | AC-2 remains partially blocked by inherited Clang feature-surface leaks; AC-3 remains unmet |
| 7 | Implemented YSX-specific Clang target-feature hardening | `YSX64TargetInfo` now owns target-feature map initialization, target-feature handling, target attribute parsing, feature-name validation, and FP/vector asm-constraint rejection. Tests cover `target("arch=+v")`, `target("arch=rv64imaf")`, explicit cc1 `+v`, and `"f"`/`"vr"` asm constraints. | AC-2 and AC-4 advanced for inherited Clang frontend feature paths; AC-3 remains unmet because backend source pruning is still queued for the next source-deletion slice |
| 7 review | Verified the Round-7 Clang hardening slice but rejected full completion | Manual review probes confirm unsupported YSX target attributes, explicit cc1 `+v`, and FP/vector asm constraints now fail before IR/backend emission. The full original plan remains incomplete because AC-3 backend source pruning is still active and YSX still carries copied vector/FP/vendor/RV32/compressed lowering, metadata, pass declarations, and compatibility helpers. | AC-2/AC-4 advanced for the reviewed frontend paths; AC-3 remains unmet and must drive the next implementation round |
| 8 | Deleted a first AC-3 backend source-pruning slice | `YSXISAInfo::parseArchString` now accepts only exact `rv64ima`, the header no longer contains broad RV32/G/C/F/D/V/Z/S/X arch parsing, stale non-IMA pass declarations were removed from `YSX.h`, the unused vector-mask DAG mutation declaration was removed from `YSXTargetMachine.h`, and MC tests cover exact `.option arch` handling. | AC-3 advanced with real source deletion; AC-2 preserved for exact `rv64ima` parsing; broader YSXVType/lowering/selection/frame pruning remains active |
| 8 review | Verified the exact parser/pass-declaration slice but rejected the full Round-8 contract completion | The Round-8 contract required removing the direct `YSXVType` forwarding layer, but `YSXISAInfo.h` still forwards RISCV vector helpers and live callers remain across lowering, DAG selection, instr-info, MC printing, and asm parsing. Review also found MC still accepts retained copied tune/profile `-mattr` flags such as `+vxrm-pipeline-flush`, `+log-vrgather`, `+single-element-vec-fp64`, and `+prefer-vsetvli-over-read-vlenb`. | AC-3 advanced but remains unmet; AC-2 has a remaining MC feature-surface leak for non-IMA tune/profile features |
| 9 | Removed the YSXVType forwarding layer and MC-visible copied tune/profile surface | `YSXISAInfo.h` no longer defines `namespace YSXVType`; MC parser/printer VTYPE text paths no longer parse or pretty-print vector VTYPE syntax; copied vector/vendor/profile tune features such as `log-vrgather`, `vxrm-pipeline-flush`, `single-element-vec-fp64`, `prefer-vsetvli-over-read-vlenb`, and `andes45` were removed or rejected by the YSX feature whitelist; negative MC coverage was added. | AC-2 advanced by closing the reviewed `-mattr` leak; AC-3 advanced by deleting the forwarding layer and feature state, while direct copied vector lowering/selection code remains active for later pruning |
| 9 review | Verified the narrow Round-9 contract but kept the full plan open | Review confirmed no remaining `YSXVType` references, `llvm-mc` rejects the reviewed non-IMA tune/profile/vendor `-mattr` flags, `-mattr=help` only lists the curated retained feature set, focused MC coverage passes, YSX smoke compile produces an ELF64 RISC-V soft-float object, and RISCV backend diff remains zero. Full Clang lit could not be rerun in the review sandbox because the external build test exec root is read-only. | AC-2 advanced for the reviewed MC feature surface; AC-3 remains unmet because copied vector/FP/RV32/compressed lowering, selection, frame, register, and metadata surfaces remain |
| 10 | Deleted a YSX vector metadata and instruction-info source slice | Removed direct `RISCVVType` exposure from `YSXRegisterInfo.h`, `MCTargetDesc/YSXBaseInfo.h`, and `YSXInstrInfo.cpp`; deleted disabled vector reassociation and unsupported FP/vector machine-combiner bodies from `YSXInstrInfo.cpp`; retained large lowering/DAG-selection deletion for a later slice. | AC-3 advanced with another source-deletion slice; AC-4 preserved by rebuilding and rerunning focused YSX tests |
| 10 review | Verified the narrow `RISCVVType` removal claim but rejected full plan completion | Review confirmed the named Round-10 files no longer reference `RISCVVType`, `git diff --check` is clean, and RISCV backend diff remains zero. However, vector TSFlags, operand kinds, verifier/printer paths, pseudo opcode tables, direct `RISCVVType` lowering/selection users, frame scalable-vector helpers, and `#if 0` compatibility bodies remain. | AC-3 remains unmet; the deferred lowering/DAG, TableGen/generated pseudo, frame, and compatibility-helper cleanup is mainline plan work, not an accepted deferral |
| 11 | Collapsed the YSX instruction-info scalar slice | Removed `YSXInstrInfo.cpp` vector/FP verifier/comment-printer handling, generated vector/FP pseudo macro families, disabled `#if 0` compatibility bodies, vector-copy option/statistics, unused FP/vector combiner stubs, and unused vector helper declarations from `YSXInstrInfo.h`; YSX-only build, focused YSX lit, combined RISCV+YSX build, smoke compiles, negative probes, and RISCV zero-diff checks pass. | AC-3 advanced with 1,639 more backend lines removed; AC-4 preserved for the focused YSX suite; full AC-3 remains active for BaseInfo/TableGen, lowering/DAG, and frame cleanup |
| 11 review | Verified the narrow instruction-info deletion slice but rejected full plan completion | Review confirmed the claimed `YSXInstrInfo.cpp`/`.h` cleanup, but the Round-11 contract explicitly queued original-plan AC-3 work: BaseInfo/TableGen vector metadata, copied vector lowering/selection, scalable-vector frame paths, and generated pseudo surfaces. Lightweight `llc` probes also show scalable-vector IR and direct `llvm.riscv.vsetvli` IR still abort in YSX codegen instead of cleanly rejecting unsupported vector input. | AC-3 remains unmet; AC-2 has residual unsupported-vector IR crash behavior; AC-4 needs new negative coverage after the pruning fix |
| 12 | Added unsupported-vector IR guard and pruned DAG/Lowering crash-surface slice | Added a YSX module-level unsupported-IR guard for scalable-vector IR and non-masked-atomic `llvm.riscv.*` intrinsics; added negative `llc` coverage; removed copied RVV/VP CodeGenPrepare transforms, DAGToDAG segment/VSETVLI/VL/splat selector helpers, vector pseudo peepholes, disabled compatibility blocks, and the unreachable RVV/SF target-memory intrinsic metadata table. | AC-2 crash behavior fixed for the reviewed unsupported vector IR paths; AC-3 advanced by 4,266 more backend lines removed; AC-4 preserved by YSX-only build, 132-test focused lit, combined RISCV+YSX build, smoke tests, and RISCV zero-diff check |
| 12 review | Verified the unsupported-vector IR crash fix but rejected the Round-12 completion claim | Review probes confirm scalable-vector IR and direct `llvm.riscv.vsetvli` now produce deterministic YSX diagnostics, `git diff --check` is clean, and RISCV backend diff remains zero. However, the Round-12 contract success criteria are not met: `YSXSelectionDAGInfo.h` still declares the named vector custom ISD nodes, `YSXISelLowering.*` still contains broad `RISCVVType`/YSXVec/vector-intrinsic lowering, and `YSXISelDAGToDAG.cpp` still has live vector insert/subvector and VMV immediate-selection paths. | AC-2 advanced for the reviewed crash behavior; AC-3 remains unmet and the lowering/custom-ISD deletion must continue immediately; AC-4 needs final revalidation after deletion |
| 13 | Deleted the residual YSX DAG selector vector/tuple and disabled compatibility slice | Removed `YSXISelDAGToDAG.cpp` vector insert/extract-subvector, tuple, `RISCVVType`, VMV vector-immediate, RV32/pseudo selection stubs, deleted disabled compatibility bodies from the asm parser, pseudo expansion, register-info, and lowering files, and removed custom-ISD compatibility entries made unused by this slice. | AC-3 advanced by another 839 backend lines; AC-4 preserved by YSX-only build, 132-test focused lit, combined RISCV+YSX build, smoke tests, negative probes, `git diff --check`, and RISCV zero-diff check |
| 13 review | Verified the narrow selector/disabled-block deletion but rejected full plan completion | Review confirmed the Round-13 targeted `YSXISelDAGToDAG.cpp` residue and `#if 0`/`if(false)` blocks are gone, and quick negative probes still reject `+v`, `rv64imaf`, scalable-vector IR, and direct `llvm.riscv.vsetvli`. However, the original AC-3 plan remains incomplete because `YSXSelectionDAGInfo.h`, `YSXISelLowering.*`, BaseInfo/TableGen/RegisterInfo/Frame/Subtarget/Disassembler/generated-pseudo surfaces still carry copied unsupported vector/FP/RV32/vendor compatibility. | AC-3 remains unmet; AC-2/AC-4 need final revalidation after the remaining source pruning |
| 14 | Deleted the scalable-vector frame-lowering state slice | Removed YSXVec stack size/alignment/padding state, scalable-vector stack-ID support, scalable CFA expression construction, YSXVec callee-save filtering/CFI, YSXVec stack probing, vector spill stack-ID assignment, and frame-scavenging helpers from `YSXFrameLowering.*` and `YSXMachineFunctionInfo.h`. | AC-3 advanced by another 546 backend lines; AC-4 preserved by YSX-only build, 132-test focused lit, combined RISCV+YSX build, smoke tests, negative probes, `git diff --check`, and RISCV zero-diff check |
| 14 review | Verified the narrow scalable-vector frame-state deletion but rejected full plan completion | Review confirmed the selected `YSXFrameLowering.*` and `YSXMachineFunctionInfo.h` YSXVec/scalable-vector frame-state patterns are gone; `git diff --check` and RISCV zero-diff pass; existing built binaries still produce YSX/RISCV ELF64 soft-float smoke objects and reject `+v`, `rv64imaf`, scalable-vector IR, and direct `llvm.riscv.vsetvli`. Full Ninja/lit could not be rerun in this sandbox because the external build directories are read-only. | AC-3 advanced; AC-1/AC-2/AC-4 maintained for reviewed probes; full AC-3 remains unmet because custom-ISD, lowering, BaseInfo/TableGen/RegisterInfo/Subtarget/Disassembler, and generated-pseudo surfaces remain |
| 15 | Deleted the unsupported custom-ISD compatibility namespace and collapsed the direct lowering slice | Removed `YSX_UNSUPPORTED_ISD` from `YSXSelectionDAGInfo.h`, deleted direct unsupported custom-ISD references from `YSXISelLowering.cpp` and `YSXISelDAGToDAG.cpp`, pruned unused vector/FP/RV32/vendor lowering declarations/bodies, and regenerated affected YSX CodeGen checks for the simplified rv64ima output. Current YSX backend line count is 33,050 versus RISCV's 136,068. | AC-3 advanced by another large lowering/custom-ISD source deletion; AC-4 preserved by YSX-only build, 132-test focused lit, combined RISCV+YSX build, smoke tests, negative probes, `git diff --check`, and RISCV zero-diff check |
| 15 review | Verified the narrow custom-ISD deletion but rejected full plan completion | Review confirmed `YSX_UNSUPPORTED_ISD` is gone and retained `YSXISD::` references in lowering/selection are rv64ima scalar nodes. However, `YSXISelLowering.*` still contains copied FP/vector/VP/GISel helper surfaces, and BaseInfo/TableGen/RegisterInfo/Subtarget/Disassembler/generated-pseudo surfaces still carry removed vector/FP/RV32 metadata. | AC-3 advanced but remains unmet; AC-2/AC-4 maintained for reviewed smoke and negative probes; task3/task6 stay active |
| 16 | Deleted the residual `YSXISelLowering.*` FP/vector/VP/GISel helper surface | Removed copied FP immediate/legalization helpers, vector/VP preference and DAG-combine hooks, `YSXVIntrinsicsTable` generated-table plumbing, vscale/scalable-vector and vector-call handling, YSXVec helpers, and GlobalISel-only fallback hooks from `YSXISelLowering.*`. Current YSX backend line count is 32,083 versus RISCV's 136,068. | AC-3 advanced by completing the Lowering source-surface cleanup slice; AC-4 preserved by YSX-only build, 132-test focused lit, combined RISCV+YSX build, smoke tests, negative probes, `git diff --check`, and RISCV zero-diff check |
| 16 review | Verified targeted Round-16 deletions but rejected full completion | Review confirmed the named Lowering markers such as `YSXVIntrinsicsTable`, generated searchable-table includes, VP combine markers, vscale hooks, and GlobalISel fallback hooks are gone from `YSXISelLowering.*`; however, residual vector-specific Lowering branches/comments remain and the non-Lowering BaseInfo/TableGen/RegisterInfo/Subtarget/Disassembler/generated-pseudo surfaces are still plan-derived AC-3 work, not accepted deferrals. | AC-3 advanced but remains unmet; task3/task6 stay active; AC-4 needs final revalidation after the remaining source pruning |
| 17 | Deleted the reviewed FP/vector metadata, parser/printer, generated-pseudo, and dead decoder surface | Removed residual vector Lowering fragments, BaseInfo vector TSFlags/operand/helper/table surface, generated vector pseudo tables, FP/vector parser/printer/disassembler hooks, `.variant_cc`, vector register metadata, VLEN/YSXVec helpers, vector-call state, the dead FP constant promotion pass, and unused decoder helpers. Current YSX backend line count is 29,861 versus RISCV's 136,068. | AC-3 advanced below the expected 30k-line target and removed the reviewed FP/vector source markers; AC-4 preserved by YSX-only build, 132-test focused lit, combined RISCV+YSX build, smoke tests, negative probes, `git diff --check`, and RISCV zero-diff check. Full AC-3 remains active for the remaining RV32/vendor false-query and compatibility branches. |

#### Active Tasks
<!-- Mainline tasks only: each task must directly advance the current round objective and carry routing metadata -->
| Task | Target AC | Status | Tag | Owner | Notes |
|------|-----------|--------|-----|-------|-------|
| task3: Finish pruning YSX to the actual `rv64ima` source surface | AC-2, AC-3 | active after Round 17 implementation | coding | Claude | Round 17 removed the reviewed FP/vector Lowering, BaseInfo/TableGen, generated-pseudo, RegisterInfo, AsmParser/InstPrinter, AsmPrinter, Disassembler, and dead FP pass surfaces. Full AC-3 remains active because source scans still show RV32 mode branches and vendor false-query compatibility callsites. |
| task6: Revalidate after source pruning and Clang/MC surface fixes | AC-1, AC-2, AC-4 | active after Round 17 implementation | coding | Claude | Round 17 validation passed YSX-only build, focused lit, combined RISCV+YSX static build, smoke compiles, negative probes, `git diff --check`, and RISCV zero-diff. Final validation remains active until the remaining RV32/vendor compatibility source surface is pruned and reviewed. |

### Blocking Side Issues
<!-- Only issues that directly block current mainline progress belong here -->
| Issue | Discovered Round | Blocking AC | Resolution Path |
|-------|-----------------|-------------|-----------------|
| Removed feature source remains renamed/disabled instead of deleted | 2 review | AC-3 | Continue replacing register/calling-convention leftovers, instruction-format metadata, lowering, frame, subtarget, MC, selection support, pass declarations, and TargetParser/Clang integration with a minimal `rv64ima`-only surface; resolve generated-code breakage by deleting unsupported callers rather than adding compatibility stubs. |
| Residual copied frame/instr/subtarget vector helpers remain after first Round-3 slice | 3 | AC-3 | Continue replacing YSX vector/scalable frame, instruction-combiner, feature, and subtarget helpers with rv64ima-only implementations; keep rebuilding after each deletion slice. |
| Residual RV32/vendor compatibility source after Round 17 | 17 implementation | AC-3 | Remove or collapse remaining `is64Bit()` false branches, RV32/ILP32 mode metadata, and vendor/removed-extension false-query callsites now that FP/vector BaseInfo/TableGen/RegisterInfo/Disassembler surfaces are pruned; rebuild after each coherent slice and keep RISCV diff at zero. |

### Queued Side Issues
<!-- Non-blocking issues stay queued and must NOT replace the round objective -->
| Issue | Discovered Round | Why Not Blocking | Revisit Trigger |
|-------|-----------------|------------------|-----------------|
| Goal Tracker immutable AC list dropped AC-3 and AC-4 from `docs/plan.md` | 0 | The mutable tracker and round summaries now explicitly track and verify AC-3/AC-4 against `docs/plan.md`; the immutable section is intentionally not edited by tracker rules. | Revisit only if a future RLCR tool requires regenerating the immutable tracker section. |
| YSX CodeGen tests retain large inactive copied check blocks for unsupported RISCV variants | 2 review | Active RUN lines scanned during review did not invoke unsupported YSX variants outside negative tests, so this is secondary to source pruning and MC rejection. | Revisit after task3/task6 are fixed, then trim or regenerate stale RV32/ZBB/XTHEAD/RV64IA-TSO check-prefix blocks that no longer describe supported YSX behavior. |
| YSX `target("cpu=...")` and `target("tune=...")` attributes still use generic warn-and-ignore diagnostics | 7 review | Manual probes show unsupported CPU/tune attributes do not leak unsupported target features into IR, so this does not block the current AC-3 backend source-pruning objective. | Revisit after AC-3 source pruning if the intended YSX policy is that every unsupported target attribute should be a hard error rather than an ignored warning. |

### Completed and Verified
<!-- Only move tasks here after Codex verification -->
| AC | Task | Completed Round | Verified Round | Evidence |
|----|------|-----------------|----------------|----------|
| AC-1 | task1: Patch Humanize hook and generate tracked draft/annotated/refined plan docs | 0 | pending verification | `docs/draft.md`, `docs/plan.annotated.md`, `docs/plan.md`, `docs/.humanize/plan_qa/plan.qa.md`, commit `ecb3eee65f06` |
| AC-1 | task2: Copy RISCV to YSX and bulk-rename backend-visible symbols/files | 0 | 0 | `llvm/lib/Target/YuShuXin/`, `llvm/lib/Target/CMakeLists.txt`, `llvm/CMakeLists.txt`; YSX-only and RISCV+YSX builds both succeed |
| AC-1, AC-2 | task4: Add LLVM/Clang `ysx64` plumbing and unique YSX option names | 0 | 0 | `llvm/include/llvm/TargetParser/Triple.h`, `llvm/lib/TargetParser/Triple.cpp`, `clang/lib/Basic/Targets.cpp`, `clang/lib/Driver/ToolChains/Clang.cpp`; combined static build + smoke tests for both targets pass |
| AC-4 | task5: Create YSX-owned LLVM and Clang tests from rv64ima-applicable RISCV subsets | 0 | 0 | `llvm/test/CodeGen/YSX`, `llvm/test/MC/YSX`, `clang/test/CodeGen/YSX`, `clang/test/Driver/YSX`; 129-test YSX suite passes |
| AC-2, AC-4 | Round-4 MC CSR/privileged/Zifencei visible-surface fix | 4 | 4 | Manual `llvm-mc -triple=ysx64-unknown-elf` checks now reject `fence.i`, `csrr`, counter aliases, `mret`/`sret`/`wfi`/`dret`, `sfence.vma`, and `hfence.vvma`; `llvm/test/MC/YSX/unsupported-features.s` contains negative coverage for these examples. |
| AC-2, AC-4 | Round-5 Clang compile-path fix | 5 | 5 review | `clang --target=ysx64-unknown-elf -c` and `clang --target=ysx64-unknown-elf -march=rv64ima -c` both produce ELF64 RISC-V relocatable objects in available YSX-only and combined builds; enabled unsupported `rv64imaf`/`rv64imac` and backend `+f`/`+v` remain rejected. Review could not rerun lit because the external build test root is read-only in this sandbox. |
| AC-2, AC-4 | Round-6 default-driver and RVV frontend probe fix | 6 | 6 review | `clang --target=ysx64-unknown-elf -### -c` now emits only `+i`, `+m`, `+a`, `+zmmul`, `+zaamo`, `+zalrsc`, and `+relax`; YSX predefines no longer include `__riscv_v_intrinsic` or `__riscv_vector`; RVV builtin/type probes fail. Review rejected the broader frontend-surface completion claim because inherited target attributes and explicit cc1 target features still leak unsupported RISCV features. |
| AC-2, AC-4 | Round-7 inherited Clang target-feature hardening | 7 | 7 review | Manual review verified `target("arch=+v")`, `target("arch=rv64imaf")`, `target("arch=+64bit")`, explicit cc1 `-target-feature +v`, and `"f"`/`"vr"` asm constraints fail before unsupported IR/backend emission; `target("arch=rv64ima")`, default driver feature emission, and normal `"r"` inline asm remain usable. Full lit was not rerun in the review sandbox because the external build test exec root is read-only. |
| AC-2, AC-3, AC-4 | Round-8 exact YSX arch parser and stale pass declaration pruning | 8 | 8 review partial | Review verified `YSXISAInfo::parseArchString` now accepts only exact `rv64ima`; `.option arch, rv64ima` passes while `rv32ima` and `rv64ima_zbb` reject; stale non-IMA pass declarations and the unused vector-mask DAG mutation declaration were removed; `git diff -- llvm/lib/Target/RISCV | wc -l` remains `0`. Review rejected full Round-8 contract completion because `YSXVType` forwarding remains and MC still accepts copied non-IMA tune/profile `-mattr` features. |
| AC-2, AC-3, AC-4 | Round-9 YSXVType forwarding and tune/profile feature-surface pruning | 9 | 9 review | Review verified `rg "YSXVType"` has no YSX matches; `llvm-mc -triple=ysx64 -mattr=+vxrm-pipeline-flush/+log-vrgather/+single-element-vec-fp64/+prefer-vsetvli-over-read-vlenb/+andes45` rejects with `YSX only supports the rv64ima ISA`; `llvm-mc -mattr=help` exposes only 64bit, I/M/A, Zmmul, Zaamo, Zalrsc, relax, and exact-asm; `llvm-lit -q llvm/test/MC/YSX/unsupported-features.s` passes; YSX Clang smoke compile produces an ELF64 RISC-V soft-float relocatable; `git diff -- llvm/lib/Target/RISCV | wc -l` remains `0`. |
| AC-3, AC-4 | Round-10 vector metadata and instruction-info pruning slice | 10 | 10 review partial | Review verified the named metadata/instr-info files no longer reference `RISCVVType`; `git diff --check` is clean; `git diff -- llvm/lib/Target/RISCV \| wc -l` remains `0`. Review rejected full completion because vector TSFlags/operands/pseudos, direct lowering/selection `RISCVVType` users, frame scalable-vector helpers, and disabled compatibility bodies remain. |
| AC-3, AC-4 | Round-11 instruction-info scalar pruning slice | 11 | 11 review partial | Review verified `YSXInstrInfo.cpp`/`.h` no longer contain the searched vector/FP verifier/comment/pseudo macro and `#if 0` compatibility-body patterns; fixed-width vector IR scalarizes to rv64ima code. Review rejected full completion because BaseInfo/TableGen metadata, lowering/selection, frame, and generated pseudo surfaces remain, and unsupported vector IR still reaches abort paths. |
| AC-2, AC-3, AC-4 | Round-12 unsupported-vector IR guard and DAG/Lowering crash-surface pruning | 12 | 12 review partial | Review verified the two new negative `llc` probes now produce deterministic YSX diagnostics instead of aborting, `git diff --check` is clean, and `git diff -- llvm/lib/Target/RISCV \| wc -l` remains `0`. Review rejected full Round-12 completion because the contract's custom-ISD/lowering/DAG-selection deletion criteria are not met. Full AC-3 remains active for custom ISD, Lowering/DAG, TableGen/BaseInfo, RegisterInfo/Frame, Disassembler, generated pseudo surfaces, and compatibility blocks elsewhere. |
| AC-3, AC-4 | Round-13 DAG selector vector/tuple and disabled compatibility deletion slice | 13 | 13 review partial | Review verified `rg` finds no `ISD::INSERT_SUBVECTOR`, `ISD::EXTRACT_SUBVECTOR`, `YSXISD::TUPLE_INSERT`, `YSXISD::TUPLE_EXTRACT`, `RISCVVType`, or `YSXISD::VMV_V_X_VL` in `YSXISelDAGToDAG.cpp`; `rg "#if 0\|if \\(false"` has no YSX matches; `git diff --check` is clean; `git diff -- llvm/lib/Target/RISCV \| wc -l` remains `0`; quick negative probes for `+v`, `rv64imaf`, scalable-vector IR, and direct `llvm.riscv.vsetvli` reject. Review rejected full completion because broad unsupported custom-ISD, lowering, metadata, frame, subtarget, disassembler, and generated-pseudo surfaces remain. |
| AC-3, AC-4 | Round-14 scalable-vector frame-state deletion slice | 14 | 14 review partial | Review verified the targeted frame-state patterns no longer appear in `YSXFrameLowering.cpp`, `YSXFrameLowering.h`, or `YSXMachineFunctionInfo.h`; deleted accessors/helpers have no remaining YSX matches; `git diff --check` is clean; `git diff -- llvm/lib/Target/RISCV \| wc -l` remains `0`; existing YSX/RISCV build products pass smoke compiles and the reviewed unsupported-feature probes reject deterministically. Full Ninja/lit could not be rerun from this sandbox because the external build directories are read-only. Review rejected full completion because `YSXSelectionDAGInfo.h`, `YSXISelLowering.*`, BaseInfo/TableGen/RegisterInfo/Subtarget/Disassembler, and generated vector pseudo surfaces remain. |
| AC-3, AC-4 | Round-15 unsupported custom-ISD and lowering deletion slice | 15 | 15 review partial | Review verified `YSXSelectionDAGInfo.h` no longer contains `YSX_UNSUPPORTED_ISD`; retained `YSXISD::` lowering/selection references are scalar rv64ima nodes; `git diff --check` is clean; `git diff -- llvm/lib/Target/RISCV \| wc -l` remains `0`; existing YSX build products pass a smoke compile and reviewed negative probes for `+v`, `+f`, and scalable-vector IR. Full AC-3 remains active because `YSXISelLowering.*` still carries FP/vector/VP/GISel helper surfaces and BaseInfo/TableGen/RegisterInfo/Subtarget/Disassembler/generated-pseudo cleanup is unfinished. |
| AC-3, AC-4 | Round-16 residual `YSXISelLowering.*` FP/vector/VP/GISel deletion slice | 16 | 16 review partial | Review verified the targeted scan has no matches for `YSXVIntrinsicsTable`, generated searchable-table includes, VP/vector combine markers, VLEN/vscale/scalable-vector hooks, vector-call conventions, YSXVec markers, FP-immediate hooks, and GlobalISel fallback hooks in `YSXISelLowering.*`; `git diff --check` is clean; `git diff -- llvm/lib/Target/RISCV \| wc -l` remains `0`. Full AC-3 remains active because residual vector-specific Lowering fragments and BaseInfo/TableGen/RegisterInfo/Subtarget/Disassembler/generated-pseudo cleanup are unfinished. |
| AC-3, AC-4 | Round-17 FP/vector metadata and generated-surface deletion slice | 17 | pending review | Implemented removal of the reviewed residual Lowering vector fragments, BaseInfo vector TSFlags/operand/helper/table surface, generated vector pseudo tables, vector register-class metadata, VLEN/YSXVec helpers, vector-call state, `.variant_cc`, FP/vector parser/printer/disassembler hooks, the dead FP constant promotion pass, and unused decoder helpers. Validation passed YSX-only `LLVMYSXCodeGen llvm-mc clang llc`, focused 132-test YSX lit, combined RISCV+YSX `LLVMYSXCodeGen LLVMRISCVCodeGen llvm-mc llc clang lld`, smoke/negative probes, `git diff --check`, and `git diff -- llvm/lib/Target/RISCV \| wc -l == 0`; current YSX line count is 29,861. |

### Explicitly Deferred
<!-- Items here require strong justification -->
| Task | Original AC | Deferred Since | Justification | When to Reconsider |
|------|-------------|----------------|---------------|-------------------|
